class Solution {
    public int[] runningSum(int[] nums) {
        int i;
        int a[]=new int[nums.length];
        a[0]=nums[0];
        for(i=1;i<nums.length;i++)
        {
            a[i]=a[i-1]+nums[i];
        }
        return a;
    }
}
