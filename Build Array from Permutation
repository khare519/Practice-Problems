class Solution {
    public int[] buildArray(int[] nums) {
        int i;
        int a[]=new int[nums.length];   
        for(i=0;i<nums.length;i++)
            {
                a[i]=nums[nums[i]];
            }
        return a;
    }
}
